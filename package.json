{
  "dependencies": {
    "hono": "^4.7.0"
  },
  "description": "Loot Radar — never miss a free game again. Free-games & deals alerts on Cloudflare Workers.",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.114.0"
  },
  "name": "loot-radar",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.js --bundle --format=esm --platform=browser --outfile=dist/worker.js"
  },
  "type": "module",
  "version": "0.1.0"
}